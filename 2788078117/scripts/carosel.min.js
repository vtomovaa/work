const track=document.querySelector(".section-9 .testimonials"),pages=document.querySelectorAll(".section-9 .page"),dots=document.querySelectorAll(".section-9 .dot"),prevBtn=document.getElementById("prev"),nextBtn=document.getElementById("next");let currentPage=0;const totalPages=pages.length;function updateCarousel(){const pageWidth=pages[0].getBoundingClientRect().width,gap=parseInt(getComputedStyle(track).gap),shift=(pageWidth+gap)*currentPage;track.style.transform=`translateX(-${shift}px)`,dots.forEach(dot=>dot.classList.remove("active-dot")),dots[currentPage].classList.add("active-dot"),prevBtn.disabled=0===currentPage,nextBtn.disabled=currentPage===totalPages-1,prevBtn.classList.toggle("disabled",0===currentPage),nextBtn.classList.toggle("disabled",currentPage===totalPages-1)}prevBtn.addEventListener("click",()=>{currentPage>0&&(currentPage--,updateCarousel())}),nextBtn.addEventListener("click",()=>{currentPage<totalPages-1&&(currentPage++,updateCarousel())}),window.addEventListener("resize",updateCarousel),updateCarousel();const headingButtons=document.getElementById("heading-buttons"),mobileContainer=document.getElementById("heading-buttons-mobile"),desktopContainer=headingButtons.parentElement;function moveButtons(){window.innerWidth<992?mobileContainer.appendChild(headingButtons):desktopContainer.appendChild(headingButtons)}window.addEventListener("resize",moveButtons),moveButtons();
//# sourceMappingURL=carosel.min.js.map